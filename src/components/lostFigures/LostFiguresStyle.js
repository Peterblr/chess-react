import styled from "styled-components";

const LostFiguresStyle = styled.div`
  width: 25vw;
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  border-radius: 15px;
  color: white;
  background: rgba(8, 8, 8, 0.7);
  height: calc(50vh - 60px);
  padding: 30px;
  margin: 20px;
`;

export default LostFiguresStyle;
